int main (void)
{
    print("hola Mundo");
    // git clone <descargar repo>
    // git fetch <una vez clonado mi repositorio descacrgar informacion>
    // git pull origin main <ya descargada informacion me mueve al ultimo comit>
    // git commit -am "mensaje" guardar los cambios que hice en mi computadoras
    // git push origin main < todos los cambios que hice los mueve a al servidor>
}